apiVersion: v1
kind: Secret
metadata:
  name: worldofgames-secrets
type: Opaque
stringData:
  # IMPORTANT: Replace this with your actual API key
  # In production, use sealed-secrets or external-secrets
  currency-api-key: "your-api-key-here"
